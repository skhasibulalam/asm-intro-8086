CODE SEGMENT
ASSUME CS:CODE,DS:CODE 
ORG 100H
MAIN: MOV AX,CS
      MOV DS,AX
      MOV AX,USER
      MOV BX,AX
      MOV DX,0
CALL RIAL
MOV AH,4CH
INT 21H
RIAL PROC
     DEC BX
     CMP BX,0
     JE FINISH
     MUL BX
     CALL RIAL
FINISH: RET
RIAL ENDP
ORG 600H
USER DW 5; find factorial of 5 using recursion
CODE ENDS
END MAIN
